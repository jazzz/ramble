# Example Ramble File
---
version: "1"              # Required
namespace: thisnamespace
packets:
  - name: hello           # Required
    properties:
      - crc
    fields:
      - seq: U16
      - flags: U8
  - name: change_record   # Required
    fields:
      - record_id: U64
      - record_value: I32
