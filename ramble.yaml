# Example Ramble File
---
version: "1"      # Required
namespace: thisnamespace
packets:
  - name: hello   # Required
    properties:
      - crc
    fields:
      - seq: u16
      - flags: u8
  - name: primitive   # Required
    fields:
      - record_id: u64
      - record_value: u32
