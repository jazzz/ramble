#Example Ramble File
---
version: "1"        # Required
namespace: thisnamespace
packets:
  - name: hello   # Required
    properties:
      - crc
    fields:
      - seq: U8 
  - name: change_record   # Required
    fields:
      - record_id: U8
      - record_value: U8
